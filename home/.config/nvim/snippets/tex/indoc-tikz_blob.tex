%%% TikZ Blob %%%
\newcommand{\blob}[2]{
\draw[fill,rounded corners=#1*1mm] (#2) +($(0:#1*2+#1*rnd)$) \foreach \a in {20,40,...,350} { -- +($(\a: #1*2+#1*rnd)$) } -- cycle;
}
%-----------------------------------------------------------------------------%
